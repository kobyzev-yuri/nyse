from __future__ import annotations

"""
Подпакет news (L1–L5): каналы, cheap sentiment, DraftImpulse, гейт, LLM-агрегат.

Реализация лежит в ``pipeline/news/*.py``. Для обратной совместимости в корне
``pipeline/`` остаются shim-модули ``channels.py``, ``sentiment.py``, …,
реэкспортирующие те же символы.
"""
