# Локальные секреты (как в lse: копия с config.env.example)
config.env
.env
.env.local

__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
*.egg-info/
dist/
build/

*.swp
.*.swp

# LLM-кэш (локальные JSON-файлы, не нужны в репо)
.cache/
